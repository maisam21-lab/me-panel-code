---
name: ksa-tracker-me-panel
description: ME Panel is a STANDALONE Streamlit app at ~/me-panel-app (user rejected tracker integration); reads the BQ bridge directly; tracker repo is local at ~/ksa-kitchenp-tracker
metadata: 
  node_type: memory
  type: project
  originSessionId: 3339f502-9ea7-4b44-b382-6531e9f9acf3
---

**ME Panel Streamlit = standalone app at `C:\Users\MaysamAbuKashabeh\me-panel-app`** (Jul 5 2026). It was FIRST built INTO the kitchen tracker ("ME Panel" section) and then REVERTED at the user's request ("I want it in a new streamlit app not within the tracker") - `git restore app/tracker_app.py` cleaned it (the tracker had other pre-existing dirty files; only tracker_app.py was reverted). Do NOT re-add it to the tracker.

**The app (`app.py`):** two tabs per country selector (ME rollup + 5): Dashboard (5 KPI st.metric cards w/ MoM deltas + 4 plotly charts: RRA/RRL/NRRA lines, Gross RR vs RR-after-MKO/MFO, CWs vs Approved bars, Occupancy vs Live Sold Rates %) and Panel (sheet-style METRICS list of (label, bridge column, fmt) x months; section-header rows; missing columns silently skipped so bridge APPENDS never break it). Data = `css-operations.me_panel_dev_us.me_sales_panel_k_monthly` (months >= 2025-01-31, <= current month) - SAME bridge as the Google Sheets panel. 15-min st.cache_data + sidebar Refresh. Decimal->float (bridge has BIGNUMERIC). Credentials: optional [gcp_service_account] in .streamlit/secrets.toml (deploy) -> ADC fallback (local; `gcloud auth application-default login` - VERIFIED works, project css-operations). ASCII-only files (user rule).

**Verified:** py_compile clean; `streamlit run` boots + serves (healthz 200, page 200); bare-mode full-pipeline run exit 0 (real BQ pull incl. charts/pivot). NO AUTH built in - README warns not to deploy publicly as-is; add an email allowlist gate if shared.

**Run:** `cd me-panel-app; streamlit run app.py` (deps exist in the tracker's venv: `C:\Users\MaysamAbuKashabeh\ksa-kitchenp-tracker\.venv\Scripts\python.exe -m streamlit run app.py`). NOTE: plotly was MISSING from the tracker venv (requirements listed it, never synced) - installed Jul 5 2026.

**Deployment (Jul 5 2026):** GitHub `maisam21-lab/me-panel-app` (private; Streamlit GitHub access needed granting) -> share.streamlit.io (main file app.py). Cloud credentials: the ONLY real SA key on the machine is `ksa-kitchenp-tracker\scripts\credentials.json` = `css-revops-analytics@eng-stg-001.iam.gserviceaccount.com` - it has NO bigquery.jobs.create ANYWHERE (Sheets-era SA), so app.py got a **query-less fallback: get_table + list_rows** (needs only dataset-level dataViewer; client-side month filtering). Maysam GRANTed `roles/bigquery.dataViewer` ON SCHEMA me_panel_dev_us to that SA via BQ console (I must not run access grants - user runs them). VERIFIED post-grant: SA list_rows = 630 rows all countries. Local `me-panel-app\.streamlit\secrets.toml` (gitignored) = ready-to-paste cloud secrets (ALLOWED_EMAILS + [gcp_service_account] generated from credentials.json by script - key never in chat). Scope must be .../bigquery NOT .../bigquery.readonly (readonly can't create query jobs).

**data-apps INCIDENT (Jul 6 2026):** on the hub, each folder under `/zpool/one/maysam.abukashabeh/` = one Okta app slot; the `dev-app` folder backed the KITCHEN TRACKER's /dev/ app. My "dev-app already exists" workaround (git init + me-panel-app remote + `git checkout -f -B main origin/main` IN PLACE) overwrote the tracker with the panel on Okta - user had to fix it. RULES: never force-checkout into an existing hub folder without checking `git remote -v` / contents first; panel lives ONLY in `/zpool/one/maysam.abukashabeh/me-panel-app` (its own slot); tracker slot must keep the tracker repo remote.

**NAMAA Copilot (Jul 6 2026):** 4th tab ":sparkles: AI Analyst" = streaming chat over the bridge (Anthropic Messages API via raw requests SSE - NO anthropic SDK dep). Key = `ANTHROPIC_API_KEY` in st.secrets (model override `AI_MODEL`, default claude-sonnet-4-6); NO key -> deterministic offline engine (alias-matched metric/country answers + auto chart) so the tab never breaks. Chart protocol: model appends one fenced ```chart JSON block (metrics<=2 col ids, countries from EXEC_REGIONS, months 3-36, line|bar) -> `_render_ai_chart` validates + draws themed plotly. Exec view also has ":sparkles: Generate AI brief" button (Claude or `_offline_brief`) + `_anomaly_scan` z-score radar chips. Hover info cards: `.mi-wrap/.mi-tip` pure-CSS tooltips (calc + behavior) on exec cards, all scorecard grid cells, deep-dive KPI values. AppTest gotcha: this Streamlit version's ButtonGroup (pills/segmented) crashes `_widget_state` on any 2nd at.run() (ValueError list.index) - monkeypatch `element_tree.ButtonGroup.indices` in tests (match by option .content); it is harness-only, browser unaffected.

**Jupyter version:** `me-panel-app/me_panel.ipynb` - self-contained notebook (no Streamlit dep) for notebooks.cssinternal.com: set COUNTRY, Run All -> pull + styled panel table + 4 plotly charts. Cell code executed end-to-end against real BQ (all values matched the verified bridge numbers). Generator script kept at scratchpad make_me_panel_nb.py pattern (cells tested = cells written). On the hub, bigquery.Client uses the hub's default creds; if access fails there, run gcloud auth in a hub terminal.

The kitchen tracker itself: repo local at `~/ksa-kitchenp-tracker`, main app `app/tracker_app.py` (~8k-line monolith), local `.streamlit/secrets.toml` has a PLACEHOLDER service account (real one only in the team deployment). The "Streamlit hands-off" feedback ([[feedback_streamlit]]) was about the TALABAT server app, not these.
