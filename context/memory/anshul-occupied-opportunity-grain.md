---
name: anshul-occupied-opportunity-grain
description: "Anshul's occupied = active closed-won occupant OPPORTUNITIES (not kitchen status); his dbt model is local at Downloads\\dbt_sales_tmp"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 7e69c21b-9c99-4c8f-9cb9-f9607dfe621e
---

Anshul's `facility_metrics_data_final.live_facilities_occupied_kitchens` is **opportunity-grain, NOT kitchen-status-grain.** Never reconcile his occupied by counting `kitchen_number__c` rows where `status='Occupied'` — that is the wrong table/grain and will miss by a handful every time (the whole "Saudi 27 = 20 Sold + 7" rabbit hole, Jun 2026, was this mistake).

**His model source is on the device** (user was right it's not a black box): `C:\Users\MaysamAbuKashabeh\Downloads\dbt_sales_tmp\models\` — a dbt project (Redshift/Postgres syntax). Key files: `occupants_metrics.sql` (occupant count), `facility_metrics_monthly_final.sql` (assembles `live_facilities_*`), `sfdc-core/sf_opportunities_staging.sql` + `sf_opportunities.sql` (field derivations), `facility_metrics_data.sql` (`facility_live` flag). The BigQuery `css-operations.sales.facility_metrics_data_final` is the LOAD-ETL output of this dbt project.

**Definition (verified):** `live_facilities_occupied_kitchens = SUM(occupants_kitchen) for facility_live` where `occupants_kitchen` = `COUNT(DISTINCT opportunity_id_18)` with: active as-of date (`actual_access_date <= asof AND (churn_date >= asof OR churn_date IS NULL)`), `closed_won` (`Closed_Won_Date__c` not null AND `StageName='Closed Won'`), `kitchen_type_cleaned='Delivery'` (= NOT pure Virtual/CloudRetail), not `%Daily/Weekly Rental%`, and `account_type_gkpis != 'Profood'` (facility `facility_type__c != 'Profood'` AND customer account name NOT IN 'Encore Spaces','Kitchen nation','Nummies - KSA'). Denominator `live_facilities_kitchen_count = SUM(facility.kitchens)` — the **total-kitchen-number field per facility** (Jad's "total kitchen numbers"), not a kitchen-row count. `sf_facilities.kitchens = account.Capacity__c` (physical kitchen capacity; includes ProFood, excludes Virtual/CloudRetail). Saudi live SUM(Capacity__c)=990 (matches Anshul). **GAP:** ~13 Saudi ProFood-only sites (the DMM cluster: Adama/Ghirnatah/Hamra/Jamiah/Manar/Rabiah/Uhud + Aqrabiya 2/Ash Shifa/Sweidi 3 NULL, Mursalat/Taawun =0) have blank/0 `Capacity__c`, so their ProFood occupant is in occupied but not in total → rate inflated ~1.3pp. Jad's call (Jun 2026): include ProFood in EVERYTHING now, split ProFood-only / CK-only later (via `kitchen_type__c`: Delivery/Commissary/ProFoods vs CloudRetail). **DECISION: change the denominator from `Capacity__c` to `account.total_kitchen_numbers__c`** (the "Total Kitchen Numbers" field) — keep ALL of Anshul's occupied logic, only swap the denominator field. **IMPLEMENTED (Jun 2026) in the proc `sp_rebuild_me_bridge` — NOT refresh_occupancy.sql.** (Lesson: I first put it in refresh_occupancy.sql, a manual step; the 12h scheduled rebuild proc overwrote it within hours — "change the proc, not the manual step", see [[me-bridge-deploy-arch]].) The proc now has a `tkn_total_all` CTE (SUM(total_kitchen_numbers__c) over live facilities per month/country + ME rollup) and the `joined` CTE sets `total_kitchens = tkn`, `occupancy = SAFE_DIVIDE(occupied, tkn)`. Deployed+rebuilt+verified: Saudi total 995 (=TKN, not 998 Capacity), occupied stays 769. The separate displayed metric `kitchens_live_facilities` ("Kitchens (Live Facilities)") still = Anshul Capacity (left unchanged). refresh_occupancy.sql is now redundant. Local proc file: `me-panel-bq/setup/sp_rebuild_me_bridge.sql`. `total_kitchen_numbers__c` is populated for every ME facility (Capacity had 15 blanks incl all 12 Saudi ProFood-only sites), and occupancy barely moves (ME 76.3%→76.3%, Saudi 77.7%→77.3%) because ProFood is fully sold (in both num & denom). Do NOT count raw kitchen records (over-counts: gave Saudi 1082 / 71% — wrong). Related fields exist: `occupied_kitchen_numbers__c`, `total_kitchens_occupancy__c`. **Open:** not-yet-live pull-forward — our bridge's latest row is 2026-07-31 (forward month-end; ties to Anshul ME 1916/1463). Two ME facilities are counted live but aren't operational today, ~0 occupied: **SA-RUH-Yarmuk** (Capacity 27 / TKN 30; SF go-live 2026-07-01; flips live exactly at 2026-07-31 in Anshul — true July pull-forward) and **UAE-AD-Al Nahyan** (10 kitchens; counted live since 2026-06-30 in Anshul BUT SF go-live reads 2026-08-31 — stale/early go-live date, ~2 months ahead). Total ~37 empty kitchens in denominator (NOT the "50" I first said — that was raw record count). **DECISION (Jad): keep Anshul's live logic unchanged — do NOT add a `go_live <= today` filter; not-yet-live facilities stay counted exactly as Anshul does.** The ONLY change to the model is the denominator field (Capacity__c -> total_kitchen_numbers__c). (Al Nahyan's stale go-live — SF says 2026-08-31 but counted live since June — is a separate SF data issue, not handled in the model.) `facility_live = go_live_date <= month_end` (monthly grain), excl inactive. Sold = `net_sold_kitchens_until_end_of_period`; for the current month his `occupied == sold` field.

**Reconstruction from css-dw-sync reproduces it** (join opp->facility by `opportunity.facility_id__c = account.id`): as-of 2026-07-31, Saudi **769 = 769 exact**, Bahrain 29 exact, Kuwait 379 vs 380, UAE 283 vs 285. The ±1-2 on KWT/UAE = the renewal/transfer **origin-opp chain** (`sf_opportunities.sql:136-137`, fed by `opportunity_origin.py` -> `cw_origin_occupancy`, a Python ETL not replicable in pure SQL). Console-ready reconstruction query saved in chat (Jun 2026). See [[sf-mirror-isdeleted]] (status-row reconciliation pitfalls) and [[me-panel-rrl-lf-only]].
