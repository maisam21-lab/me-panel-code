---
name: me-facility-standalone-split
description: "ME per-country FACILITY tabs live ONLY in standalone files, never the master sheet; buildFacilityPanel_ is deliberately redirected to the standalone build — don't 'restore' it"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3339f502-9ea7-4b44-b382-6531e9f9acf3
---

Jad Jul 2026: the per-country **facility** detail tabs ("Facility — <country>") were removed from the master ME workbook to keep it light and non-duplicated. They now live ONLY in the standalone files ("ME Facility Panel - <country>", opened by `ME_STANDALONE_ID_*` script properties). The master keeps only the country-grain artifacts (Full Panel, Summary, Cloud Retail, Metric Book) via `buildMEPanel_v2`.

**Foot-gun / do not undo:** every facility-tab build path funnels through `buildFacilityPanel_(country)` in `~/Desktop/me_facility_panels.gs`. It is DELIBERATELY redirected to `return buildStandaloneCountryFile_(country)` — it used to `renderFacilityPanel_(getWorkbook_(), ...)` which wrote a tab into the MASTER (the recurring "new sheets appeared in the ME sheet" leak). Do NOT restore the master-render body.

Leak had 3 vectors, all now closed: (1) nightly `meNightlyStep1-4` — migrated to `safeBuildStandalone_` / `meScheduleOneOffStandalones_`; (2) one-off triggers `meOneOffFacility*_` / `meScheduleOneOffFacilityRebuilds_` — now orphaned (nothing schedules them) and redirect-safe anyway; (3) the "Facility panels (per country)" menu submenu in `me_panel_complete.gs` — removed (the "Standalone files" submenu remains). `renderFacilityPanel_` is SHARED by the standalone path — don't delete it.

Run `removeCountryFacilityTabsFromMaster()` ONCE after deploying to delete the existing 6 tabs from the master; the refresh paths no longer recreate them. Relates to [[me-live-sold-history-fix]], [[ksa-tracker-me-panel]], [[feedback-verify-not-guess]].

**QC facility tagging (Jul 10 2026, Maysam):** the is_live_account fix surfaced 21 live 0-kitchen facilities — data showed 20 are `facility_type='QC'` + 1 Mixed-Use, ALL TKN=0. **QC = Quick Commerce** (a business LINE — NOT "Quality Control" / pre-build; corrected from an earlier wrong assumption). These facilities have 0 delivery (`K##` "Hot Kitchen") units; their occupied unit is a Quick-Commerce/Retail `S##` kitchen (e.g. Mursalat S01, occupied by Noon, ~62k SAR), which the Delivery-only panel excludes from kitchen counts.

**Delivery-vs-QC classification split (Jul 10 2026, OPEN — user dismissed the decision, awaiting direction):** the occupied `S##` (Quick-Commerce) units have their OPPORTUNITY tagged `kitchen_type_cleaned='Delivery'`, so their LF IS in delivery Gross RR (gross_rr_base deliberately includes non-K occupants), but `occupied_kitchens` (K-only kitchen-status count) excludes them → revenue counts, occupancy shows 0. Systemic: **19 occupied Delivery-tagged opps on non-K kitchens across ME, ~689k local LF, 1 per facility** (matches the gross_rr_base "17 non-K = 637k SAR" note). Options (count-as-occupied w/ kitchen-count TKN denom / keep delivery-only / reclassify at source) — **PARKED Jul 10 2026: Maysam is checking with Jad, awaiting his call.** Do NOT change occupancy methodology until Jad weighs in. Maysam's call: KEEP them but flag them. Wiring: `sp_rebuild_me_facility` now surfaces `facility_type` (from `acct_fac`.`a.facility_type__c`) as appended bridge col **135**; `buildMonthFacilityMap_` reads it into `rec.facilityType` (const `FACILITY_TYPE_COL=135`); `tagQcFacilities_` renames QC facilities to `"<name> - QC"` in BOTH the facilities list and byFac keys (safe: injection loops run on original names BEFORE the tag; the guarded `writeFacilityStatusNestedBlock_` status lookup yields blank for the renamed key); `colorQcLabels_` colours those col-A labels purple `#8e24aa` bold after styling. `QC_TAG=' - QC'`. Only `renderFacilityPanel_` (the standalones) tags; `renderCombinedFacilityPanel_` (Bahrain/Qatar, 0 QC) untouched. USER: pullExtractF (col 135) + paste me_facility_panels.gs + rebuild.

**Occupancy numerator/denominator asymmetries (Jul 12 2026, verified — same Jad-parked occupancy question):** country occupancy = Occupied Kitchens (mart `occupied_kitchens_gkpis`) ÷ Total Kitchen Numbers (`SUM(account.total_kitchen_numbers__c)` over LIVE facilities: `go_live_date__c <= month_end` AND not inactive). Numerator VERIFIED = our occupant-opportunity grain exactly (Jun ME 1484=1484). TWO ways the numerator picks up occupants the denominator misses: **(1) ancillary/non-K units** — ~18 (Jun) occupied on retail/QC `S##` kitchens at LIVE facilities; in the numerator, but their space isn't in the delivery-only TKN denominator. **(2) partially-live facilities** — occupied kitchens at facilities whose account go-live is in the FUTURE. MONTH-DEPENDENT: Jun=0, **Jul=4, all at `SA - RUH - Sweidi (4)`** (go-live 2026-10-01, account TKN 16, kitchens accessed mid-Jul). The 4 are in Jul numerator (part of 1476) but Sweidi's 16 TKN isn't in the denominator (1861) → Jul occupancy 79.31% is ~0.7pp overstated; consistent = add Sweidi TKN (1476/1877=78.6%) OR drop the 4 (1472/1861=79.1%). Console query for occupied-in-non-live saved pattern: sf_opportunities Delivery closed-won, `actual_access_date<=month_end` & not churned, JOIN account WHERE `go_live_date__c IS NULL OR >month_end OR inactive<=month_end`, dedup per kitchen. NOTE: current bridge Jun occupancy ~80% (1484/1853), NOT the 71.66% in Qazim's screenshot (that was a stale/older render).

**PARTIALLY-LIVE DECISION IMPLEMENTED (Jul 12 2026, Maysam decided — count partially-live even if facility not fully live):** `tkn_total_by_market` in `sp_rebuild_me_bridge` now includes a facility's FULL account TKN when `go_live_date__c <= month_end` **OR** `partialgolivedate__c <= month_end` (still excl inactive). Reads the SF field `partialgolivedate__c` (Sweidi (4) = 2026-07-07; `numberofkitchenspartialgolive__c` is NULL in SF, so we use full account TKN, not a partial count). Deployed+rebuilt+verified: Jul ME TKN 1861→1876, occupancy 79.31%→78.68% (occupancy goes DOWN because the 4 occupied were already in the numerator; the change adds Sweidi's 12 VACANT kitchens to the denom — corrects an overstatement). Jun unchanged (Sweidi partial date Jul 7 > Jun 30). This denom also feeds the Live-Sold Rate family (rates dipped slightly, stay <100%). USER must re-pull Extract_K + rebuild panel. **STILL TODO (flagged, not done): the FACILITY bridge `sp_rebuild_me_facility` acct_fac / is_live_account still use go_live only — apply the same partialgolivedate for standalone-panel consistency.** The separate ancillary/non-K occupancy gap (~18 units) remains PARKED for Jad.

**"Approved Deals (Live)" metric added (Jul 12 2026, Qazim ask):** new country-bridge output col **156** `approved_deals_live` = the Approved Deals count (same approved_base filters: ME-5, excl Virtual/CloudRetail, Stage Approved/Closed Won, excl Member Transfer, by Date_Approved) restricted to facilities LIVE (or partial-go-live) & not inactive as of the approval month (joins account `aa` on `co.facility_id`; `is_live_fac` flag). Deployed+rebuilt+verified: ME Jun 91 of 102 total (11 at non-live), Jul 18 of 19. Panel `me_panel_complete.gs`: wired `approvedDealsLive` (SRC 156, format '0', reads/null/color/registry). Maysam wanted it as a BREAKDOWN, not a standalone row -> it's a display-only FEEDER `{ field:'approvedDealsLive', meKind:'sum', label:'↳ Approved Deals (Live)' }` inside the `liveSoldRateApproved` nestedProd block (5th sub-row, after Vacant w/ Approved Opp, before Total Kitchen Numbers). NOT in GLOBAL_FULL_PANEL_ORDER, no standalone spec. Safe because nestedProd reads the pre-computed rate via `fromExtractME` (headRatio absent), so the extra feeder doesn't affect the headline. Distinct from "Vacant w/ Approved Opp" (live_vacant_appr_k): that's a month-end STOCK of vacant live kitchens w/ approved opp (Jun 50); this is a FLOW of approved deals at live facilities (Jun 91). USER: pullExtractK (col 156) + paste me_panel_complete.gs + buildMEPanel_v2.

**Apps Script "Out of memory" on standalone build (fixed Jul 10 2026):** the facility render loads all of Extract_F via `getDataRange().getValues()` and pivots months into columns. Extract_F had 105 months (2018→2026, 13,860 rows) but the panel only renders from `PANEL_START_MONTH = '2023-07-31'`; the +15 Saudi live-0 facilities (from the is_live_account fix) tipped the already-heavy build over the heap limit. FIX: added an output-only `WHERE s.month_end >= DATE('2023-01-31')` to the FINAL SELECT of `sp_rebuild_me_facility` (facility_spine is used only there; every metric CTE reads `month_spine`, so 2023+ values are unchanged — 6-mo lead buffer kept for deltas). Extract_F 13,860→5,940 rows (−57%). To raise the window later, change BOTH the bridge WHERE and PANEL_START_MONTH. USER must **re-pull Extract_F** (`pullExtractF`/`meHardRefreshNow`) BEFORE building, else the render still reads the old big grid and OOMs.

**SECOND OOM cause — bloated sheet GRID (fixed Jul 10 2026):** after the row trim, UAE standalone STILL OOM'd. Diagnostic (`sh.getMaxRows()`) showed the sheet GRID was **22,044 rows** even though data was only 5,941 — a prior build / the connector left the grid bloated. The render's `clearContent()/clearFormat()` ran over `getRange(1,1,sheet.getMaxRows(),...)` = the whole 22k-row grid, and THAT allocation OOM'd (the getDataRange READ is fine — it uses lastRow=5941). Saudi worked only because its standalone had a freshly-rebuilt lean grid. FIX: new helper `trimAndClearSheet_(sheet, needR, needC)` in me_facility_panels.gs — unfreezes, breakApart(used extent), `deleteRows`/`deleteColumns` to shrink the grid to just over what the panel needs, THEN clears. All 3 render funcs route through it (renderFacilityPanel_, renderCombinedFacilityPanel_, and the AE/data writer ~line 1929). This is a **.gs-only fix — NO re-pull needed**; first rebuild also permanently de-bloats the sheet. If OOM ever recurs: build per-country (not `buildAllStandaloneCountryFiles`) or window the rendered months tighter.
