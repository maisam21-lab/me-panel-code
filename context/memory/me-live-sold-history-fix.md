---
name: me-live-sold-history-fix
description: Live-Sold family - closed months use global-mart contracted-kitchen history (sf_kitchens.status has NO history); current/future = status book; churning=0 historically
metadata: 
  node_type: memory
  type: project
  originSessionId: 3339f502-9ea7-4b44-b382-6531e9f9acf3
---

**Live-Sold Rate history fix (deployed Jul 6 2026, Jad "not accurate / can't be the same"):**

- Root cause: `sf_kitchens.status` is a snapshot - Salesforce keeps NO kitchen-status history (no history table for `kitchen_number__c` in css-dw-sync; verified). Old bridge counted TODAY's statuses for every month -> flat series (Bahrain 76.32% x 14 mo; ME numerator frozen 1473 May-Sep).
- Fix v2 (same day) in `sp_rebuild_me_bridge()` (ck_kitchen_month + gk_live_sold CTEs + CASE blend): **CLOSED months** count **DISTINCT kitchens with an active CW** (cw <= m < churn; accessed = access date <= m) inside our own live-kitchen universe, over the TKN denominator. **Current + future months** keep the live status book (ties to the SF Kitchens report; ME seam 0.75%).
- v1 used the global mart's `live_facilities_kitchen_sold_count` - WRONG: that column counts CONTRACTS (net CW arithmetic), not kitchens; kitchens with 2+ concurrent CW records (renewals booked without churning the old opp - 13 in UAE Aug-23) pushed UAE 2023-24 over 100% (104%). Maysam caught it. Mart inflation was ~3pp in EVERY market. v2: 0 months > 100%, UAE Aug-23 = 94.2%.
- Consequences by column: closed months report `live_churning_k = 0` (mart cannot split churning; those kitchens sit inside `live_occupied_k`), `live_sold_k` = contracted-not-yet-in = GREATEST(gk_sold - gk_occ, 0); `live_true_sold_rate` == `live_sold_rate_approved` for closed months. Column positions unchanged - sheet .gs untouched, just re-Pull extracts.
- v3 (same day): historical CHURNING split reconstructed via point-in-time replay of Churn_Date__c from `opportunityfieldhistory` (tracked since Jun 2021): churning at m = accessed occupant whose churn date ON RECORD AT m is > m; cancelled churns drop out; fallback for no-history opps = churn_notification_date__c <= m. Validated 96 vs 86 live book (BH exact). Closed-month trio: sold = contracted-not-accessed, occupied = accessed-not-churning, churning = replay.
- v4 (same day, Maysam's catch): live_vacant_appr_k history was vacancy-TODAY x approved-window -> ~5x UNDERCOUNTED past (ME true 40-54 vs 7-12 shown; BH Oct-25 = 6 not 0). Closed months now: vacant at m = in live universe at m AND absent from contracted set (ck_kitchen_month) - crossed with appr_kitchen_month. Approved/true rates recompute. Current month stays status book.
- v5+v6 (same day, Jad decree "our TKN is the correct one"): the WHOLE kitchen-count family now uses OUR universe - `kitchens_live_facilities` = TKN, `kitchens_non_live_facilities` = nl_kitchens_total (scheduled-go-live sites), `kitchens_all_facilities` = sum (mart fallback only when both null), Verified Live+NonLive=All in all 6 markets (BH 38+9(Galali)=47; ME 1854+180=2034). Mart kitchen dimension drifts from SF by units per facility - never mix it with TKN in one ratio. EXCEPTION (user: "these are out already"): `true_sold_rate` KEEPS the published mart denominator (fm.kitchens_all_facilities, ME Jun = 0.5405) - do NOT resync it to the TKN+non-live sum without a formal restatement; known consequence: committed/All-row on the sheet (65.9%) != the rate row (54.1%).
- v7 (Jul 8 2026, multi-agent audit): fixed a regression FROM v6 - `kitchens_non_live_facilities` used COALESCE(nls.nl_kitchens_total, fm.nonlive), leaking the mart broad value (Saudi Feb-May 2026 = 330) when the narrow non-live pipeline emitted no row, breaking Live+NonLive=All on 5 rows. Now all three fields use the SAME CASE branch (COALESCE(nl,0)). Verified 0 invariant fails. Country bridge otherwise PASSED the audit: no rate>100%, v2/v3/v4 all present (churning off-by-1 in 3 months = minor rounding, unfixed).

**FACILITY BRIDGE - Phase 1 DONE (Jul 8 2026): Live-Sold family added at facility grain.** `sp_rebuild_me_facility` now 133 cols (canonical copy at `me-panel-bq/setup/sp_rebuild_me_facility.sql` - previously BQ-only). Appended cols 127-133 (live_sold_k/occupied_k/churning_k/vacant_appr_k + live_sold_rate/_approved/_true) NON-BREAKING (facility_id/name stayed 125/126). Ported country ck_km_fac/gk_ls_fac/churn-replay/vac_ah_fac/lsm_fac to facility grain; denominator = facility physical total_kitchens (occ.total_kitchens), so every facility rate <=100% (verified 0 rows >1.05). Reconciliation to country: after INCLUDING BeSpoke (BP = BeSpoke, NOT business-park; Maysam Jul 8 "report the same as in countries to facilities") by removing both `!= 'BP'` filters from the facility proc, **live-sold reconciles EXACTLY for ALL markets incl Saudi (768=768, was -222)**. BeSpoke facilities now appear as facility rows. NOTE: occupancy/total_kitchens still diverge per-facility (KW +8, SA -12, UAE +11 occupied) = SEPARATE pre-existing definitional differences (status-snapshot occupancy + TKN-vs-physical-count), not caused by the BeSpoke/live-sold change.
**Phase 2 DONE (Jul 8 2026):** the facility panel's live-sold block was NOT read from Extract_F - it's INJECTED via `pullLiveStatusByFac_()` in me_facility_panels.gs (a direct BQ query), which computed the STATUS SNAPSHOT inline (frozen history). Fixed by swapping that query to read the fixed facility bridge: `SELECT ... live_sold_k AS sold, live_occupied_k AS occ, live_churning_k AS churn, live_vacant_appr_k AS vac_appr FROM me_sales_panel_k_facility_monthly` (same 7-col shape, JS loop untouched). Verified the query returns MOVING event-based history (BH UAH occ 6->15->19->20->18). This ONE .gs change fixes BOTH the in-workbook facility tabs AND the standalone country files (same renderFacilityPanel_/ctx path). FACILITY_PANEL_ORDER already had liveSoldRate/liveSoldRateApproved. USER ACTION: paste updated me_facility_panels.gs into Apps Script + run meHardRefreshNow. (Cols 127-133 on Extract_F are now redundant-but-harmless; the panel uses the injection, not the extract positions.)
**Remaining facility accuracy gaps NOT yet addressed (audit, lower priority):**
1. occupancy/occupied_kitchens/occupied_kitchen_space/sold_status_kitchen_space (cols 15,16,18,24,19) computed from `status_current` = TODAY's sf_kitchens.status snapshot -> STATUS-SNAPSHOT DISEASE (needs v2/v3/v4 event-based CTEs ported to facility grain; ck_kitchen_month already has facility_id via sf_kitchens.facility_id_18, reusable with GROUP BY facility).
2. Whole live-sold/true-sold family absent (facility stops at col 126) -> facility panels can't show any of Jad's fixed rows.
3. fx bug: facility opp_base cw_lf_usd uses ONLY facility-country fx (line 98), never record currency -> same class as the country RRX 3x bug; wrong for non-local-currency facilities (Hessa, Ardiya - also hit by mart-label normalization mismatch lines 331-337).
4. sold_rate_live/sold_kitchens_live (cols 100-103) = raw uncapped mart passthrough -> can exceed 100%.
5. FIXED Jul 9 2026: kitchens_live/non_live/all (facility cols 94-96) were mart passthrough = CAPACITY/Total-Spaces (Deira showed 26 vs TKN 23; diverged on 29 facilities). Now = occ.total_kitchens (COUNT of sf_kitchens records = TKN for 109/110; live/non-live split preserved from mart classification). Verified 110/110, 0 rates>100%, splits intact. Deployed sp_rebuild_me_facility + rebuilt. USER DEPLOY: re-pull Extract_F + rebuild facility panels. Tried account.total_kitchen_numbers__c (like country tkn_total_by_market) but REVERTED - account TKN is occasionally understated vs actual kitchens, pushing 6 facility-months' sold/live-sold rates >100%; physical COUNT is the safe denom (always >= sold/occupied). DATA FLAG: SA-DMM-Rawda has 23 sf_kitchens rows but account.total_kitchen_numbers__c=22 (stray record or stale account field) - panel shows 23. bq CLI on Windows crashes PRINTING proc results (UnicodeEncodeError cp1252); set $env:PYTHONIOENCODING='utf-8' before bq query - the job still succeeds server-side.
6. FIXED Jul 9 2026 (Jad: Mursalat missing): facility ROSTER is now go-live-driven, not kitchen-status-driven. Root cause of the gap: the `facilities` CTE was DISTINCT from kitchen_universe, which requires kitchen_full_name LIKE 'K%' AND status IS NOT NULL - so a live facility whose K-kitchens have NULL status (e.g. SA-RUH-Mursalat: 23 K-kitchens all status=NULL in SF, verified in live css-dw-sync.kitchen_number__c too, not stale) never entered the roster. FIX: added acct_fac CTE (live ME facilities from account: go_live_date<=today, not inactive) and made `facilities` = kitchen_universe UNION acct_fac (dedup by facility_id, prefer ku name for the mart join). Roster grew Saudi 41->56 (+15), UAE 33->39 (+6); Kuwait/Bahrain/Qatar unchanged (already complete - Maysam's recollection). New facilities show EMPTY (occupied=0) until SF kitchen status filled. ALSO (Maysam decree "TKN we just read it as a field"): facility TKN/rate denominators switched from occ.total_kitchens (COUNT) to COALESCE(af.tkn, occ.total_kitchens) = account.total_kitchen_numbers__c (matches country tkn_total_by_market). Rates left UNCAPPED (Maysam) - ~6 facility-months read >100% where account TKN < actual sold. Consequences: Mursalat TKN=0, Rawda 22 (account) not 23 (COUNT). This REVERSES the Jul 9 earlier revert (which used COUNT to avoid >100%); Maysam explicitly chose the account field + uncapped. Join key confirmed: account.id = bridge facility_id.
6b. FIXED Jul 10 2026 (Mursalat STILL not showing after fix #6): the roster fix put Mursalat in the BRIDGE, but a RENDER-level filter hid it. `facilityListForCountry_` in me_facility_panels.gs only listed a facility if total_kitchens>0 OR cws>0 OR occupied>0 in the window — so live 0-kitchen facilities (Mursalat + 20 others, whose account TKN reads 0) never rendered. FIX (Maysam chose "show live facilities at 0", keep TKN=field): appended `is_live_account` = `CASE WHEN af.facility_id IS NOT NULL THEN 1 ELSE 0 END` as facility bridge col **134** (reuses the existing `af`=acct_fac join, so it flags currently-live account facilities); `buildMonthFacilityMap_` reads col 134 into `rec.isLiveAccount` (const `FACILITY_IS_LIVE_ACCOUNT_COL=134`); `facilityListForCountry_` now includes a facility if activity>0 OR `isLiveAccount`. Verified: Saudi 41->56 (+15), UAE 33->39 (+6) now RENDER (incl Mursalat), `still_hidden_dead_zero=0` (no dead facilities leak in — every 0-activity roster facility is live). Deployed+rebuilt sp_rebuild_me_facility. USER: paste me_facility_panels.gs + run meHardRefreshNow (re-pulls Extract_F w/ col 134). See [[me-facility-standalone-split]] for where these render (Saudi/UAE standalone files, not master).
REFUTED by verify (do NOT chase): country sold_rate_live>1.0 (deliberate mart passthrough, not our bug); facility kitchens_* "fully frozen/no history" (data shows partial history); panel_v2_extended.gs auto-manifest (can't, but banner-marked SUPERSEDED anyway).

**OCCUPANCY BREAKDOWN (Jul 8 2026, Qazim flag "what are Live/All Facilities under Occupancy"):** the Occupancy nestedProd block's feeders were liveFacilities/allFacilities (facility COUNTS, confusing). Changed to occupiedKitchens/totalKitchens in me_panel_complete.gs (getWebBootBlockDefs_ ~line 1345) + removed standalone occupiedKitchens from GLOBAL_FULL_PANEL_ORDER (Full) - Summary/CR inherit via shared block. NOTE the occupancy % itself was ALWAYS correct (occupied/TKN, live facilities only) - only the expand-breakdown display changed. STATUS: rebuild ran OLD code (fix in Desktop file + pasted but panel still shows Live/All - likely unsaved Apps Script editor copy OR a DUPLICATE getWebBootBlockDefs_ across .gs files; Ctrl+F "↳ Live Facilities" in the editor to find). PER-FACILITY occupancy breakdown: DONE on disk (Jul 8 2026) - me_facility_panels.gs: added `occupancy: 1` to FACILITY_FORCE_PER_FACILITY + removed the special-case that dropped occupancy's feeders (~line 307). Occupancy now routes to writeFacilityStatusNestedBlock_ -> country headline from Extract_K, each facility's headline + ↳ Occupied Kitchens/↳ Total Kitchen Numbers from its Extract_F record (occupied_kitchens col16 / total_kitchens col29, both already present). Balance-checked. DEPLOYS when user pastes me_facility_panels.gs into Apps Script + rebuilds facility panels - do AFTER the country occupancy fix lands.
ROOT CAUSE of "still shows Live/All Facilities": the Apps Script PROJECT's me_panel_complete.gs still has the OLD occupancy block (nestedProd + liveFacilities/allFacilities feeders); the disk/chat copy has the fix but was never actually saved into the project. NOT a duplicate-function shadow (the other 4 getWebBootBlockDefs_ copies - recovered_apps_script.gs, Code_gs_full_fixed.gs x2, apps-script-deploy/Code.gs - all have an OLDER spaceRate occupancy w/ NO feeders, so they can't produce Live/All). FIX = surgical 2-line feeder swap in the Apps Script editor's me_panel_complete.gs (liveFacilities/allFacilities -> occupiedKitchens/totalKitchens), Ctrl+S, buildMEPanel_v2. Also delete stale recovered_apps_script/Code_gs_full_fixed files if they're in the project.

**PLUMBING (audit Jul 8):** (a) standalone country files were never auto-refreshed - now added to auto_refresh.gs chain (autoStep_std_*). (b) auto_refresh.gs refresh step was refreshAllDataSources() = NO-OP on plain-grid extracts -> fixed to procs + pullAllExtracts (real pull); this file now supersedes meNightlyStep1 (install only ONE). (c) EXTRACT_HEADERS_EXPECTED in me_panel_complete.gs still 124 entries vs 153 cols -> cols 125-153 (incl Live-Sold family) have NO header tripwire (unfixed - flagged). (d) panel_v2_extended.gs stale 124-col landmine -> banner-marked SUPERSEDED.

- OPTIONAL follow-up (offered, not built): daily `kitchen_status_snapshots` table would restore the true churning split for months after snapshots begin + give an SF-book audit trail.
